module.exports = function (...nomes) { // os ... servem pra dizer que o argumento vai variar, podendo passar quantos nomes quiser
    return nomes.map(nome => `Boa Semana! ${nome}!`) // pode cirar função, classe, objeto, array, a porra toda, javascript = mtu versatil e me likey likey likey likey 
}